# Motor InsightFace (opcional). Se instala SIN resolver dependencias, porque todo está fijado aquí:
#   pip install --no-deps -r requirements-insightface.txt
# insightface 2.0 declara `opencv-python`, que chocaría con `opencv-python-headless`: por eso no se
# usa un `pip install -r` normal. `pip check` avisará de que falta `opencv-python`; es lo esperado.
# El motor SFace no necesita nada de esto: usa el OpenCV que ya está en requirements.txt.
-r requirements.txt
certifi==2026.7.22
charset-normalizer==3.5.1
colorama==0.4.6
flatbuffers==25.12.19
ImageIO==2.37.4
insightface==2.0
lazy-loader==0.5
ml_dtypes==0.6.0
networkx==3.6.1
onnx==1.23.0
onnxruntime==1.30.0
packaging==26.3
pillow==12.3.0
protobuf==7.36.2
requests==2.34.2
scikit-image==0.26.0
scipy==1.18.1
tifffile==2026.9.15
tqdm==4.70.1
urllib3==2.8.0
